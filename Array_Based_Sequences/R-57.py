def find_repeated_integer(A: list[int]) -> int:
    """
    Finding the integer in A that is repeated.
    
    Assuming A is an array of size n >= 2 containing integers from 1 to n-1 inclusive, 
    with exactly one repeated.
    """
    pass
